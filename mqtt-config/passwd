# MQTT User Credentials (password-based auth)
# Generate with: mosquitto_passwd -c passwd username
# For development, create users:
# iot_user:iot_password
# admin_user:admin_password

# Note: In production, use certificate-based authentication (TLS)
